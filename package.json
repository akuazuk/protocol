{
  "name": "protocol-e2e",
  "private": true,
  "description": "E2E-проверки интерфейса Protocol (Playwright). Приложение на Python, npm нужен только для тестов.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
