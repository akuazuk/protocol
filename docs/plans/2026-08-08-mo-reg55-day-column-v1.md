# МО: колонка и разбор по пост. МЗ №55 в таблице дня

Статус: **active**  
Дата: 2026-08-08

## Контекст

Детерминированная оценка `evaluate_reg55` уже кормит ось regulatory, но в таблице
«Все случаи» и в разборе кейса не было отдельного столбца и пошагового списка
пунктов. Нужен явный % №55 и раскрытие с номерами пунктов / вердиктами.

## День с полными данными (GCE warehouse)

Рекомендуемый день для просмотра: **2026-08-05**  
(~575 МО, ~489 с осью regulatory / №55). Также полные: 03, 04, 06.08.

## Что изменено

1. `reg55_criteria.py` - полный `criteria[]` (verdict, point, score, how_checked),
   `fields_present_from_case`, `attach_reg55_to_detail`.
2. `mo_backend` - `reg55_pct` в списке cases (join `fact_mo_score_axis`).
3. Case detail API - live `reg55` в payload.
4. UI: колонка «№55», сортировка, блок в «Подробнее».

## Метрики

| | Было | Стало | Цель |
|--|--|--|--|
| Колонка №55 в таблице дня | нет | есть (`reg55_pct`) | видно на полном дне |
| Разбор пунктов при открытии | только finding text | таблица criteria | все applicable + na |

## Шаги

- [x] API list + detail
- [x] UI column + expand
- [x] Тесты criteria detail
- [ ] PR + deploy GCE
- [ ] Smoke: дата 2026-08-05 → колонка заполнена → открыть кейс

## Риски

- Без `block_scores` критерии alignment остаются `na` (не fail) - честно.
- % - прокси case-level, не полная экспертиза организации по №55.
