# Лаборатория клиента в МО из `mis_tests` (v1)

Статус: **active**  
Дата: 2026-08-26  
Связанные: история пациента `2026-08-08-mo-patient-history-bundle-v2.md`,
concordance `2026-08-05-mo-eval-smirnova-concordance-v1.md`,
ночь GCE `2026-08-07-by-home-gcp-llm-split-v1.md`.

Канон: **анализы клеим к клиенту и окну дат, не к `visit_id`.**
Пустой блок лучше, чем чужие результаты. В слот «Данные обследований»
не пишем - это текст врача и №55.

---

## 1. Контекст

В МИС `kravira_mc` с 21.08 есть таблица `mis_tests` (~2.24 млн строк,
2022-05-17 … живая дата). `visit_id` нет. Ключ: `patient_id` (тот же, что
в `mis_protocol` / `mis_data`) + `date`. Строка = показатель внутри заказа
(`test_id` / `type_name` / `indicator_name` / `value` / `unit`). Референса
и флага нормы нет.

Склад МО Аналитики (GCE `mo_analytics.sqlite`, 26.08):

| | |
|--|--|
| Случаев | 105888, все в 2026 |
| Даты | **2026-01-02 … 2026-08-25** |
| Clinical+consultation | 83673 |

Покрытие (без PHI): same-day лаборатория у ~16% визитов с протоколом
(1480/8971 за 26.07-13.08). Окно −14д … день визита на срезе 01.08:
104/308 (~34%). Поэтому lookback 14 дней обязателен.

`mis_tests` за период склада: **410616** строк (2026-01-01…08-25);
с буфером −14д/+1д: **427045**.

---

## 2. Что меняется в проде

Волна 0: данные на диске GCE. Волна 1: бандл + блок в разборе; score не трогаем.

Целевой файл (отдельный, чтобы не лочить BI-sqlite):

`/var/data/medical_exams/warehouse/mo_lab.sqlite`

Таблица `fact_mo_lab`: `patient_key` (sha256[:20] как в `mo_daily`),
`test_date`, `test_id`, `type_*`, `indicator_*`, `value`, `unit`.
Сырой `patient_id` на складе не храним.

---

## 3. Метрики

| Метрика | Было | Стало (GCE 26.08) | Цель v1 |
|--|--|--|--|
| Лаб на складе МО | 0 | **427045** строк, `mo_lab.sqlite` 91 MB | держать ночью |
| Ключи пациентов с лаб | 0 | 10239 | - |
| Пересечение с `fact_mo_case` | 0 | **9340 / 51917** ключей | не цель 100% |
| Случаи с лаб в окне −14д…+1д | 0 | **23121 / 105888 (21.8%)** | честно показывать пусто |
| `exam_data` врача | как есть | не трогали | не переписываем |
| Primary score | без лаб | без лаб | `MO_LAB_IN_PRIMARY=0` |

---

## 4. Волны

0. **[x] Backfill на GCE** за период склада + lookback 14д
   (2025-12-19 … 2026-08-26). `scripts/ingest_mo_lab_from_mis_tests.py`.
   Файл `/var/data/medical_exams/warehouse/mo_lab.sqlite` (427045 строк).
   Отчёт: `/var/data/medical_exams/reports/mo_lab_ingest_meta.json` (без PHI).
1. **[x] Бандл + блок «Лаборатория»** в разборе случая
   (`clinical_knowledge/mo_lab_bundle.py`, `result.lab`, UI рядом с историей).
   Группировка дата → `type_name` → показатели. Live SQL на клик нет.
   `MO_LAB_BUNDLE=1` по умолчанию; `MO_LAB_IN_PRIMARY` в v1 игнорируется.
2. Shadow-сверка: «Рекомендации по обследованию» ↔ `type_name` (ОАК назначен /
   ОАК есть). Finding только shadow.
3. Night: дописывать вчерашний день в том же sqlite из `night_mis_pipeline.sh`.
   Индекс МИС `(patient_id, date)` - просьба к владельцу МИС, не блокер склада.
4. Primary / «плохой анализ» из `value` - **не в v1** (нет референса).

---

## 5. Как анализировать МО точнее после волны 0-2

- Отличить «врач не написал обследование» от «пациент уже сдал ОАК/БАК».
- Concordance диагноз ↔ цифры: методист видит панель, движок пока не штрафует
  отклонения без калибровки 30 кейсов.
- Не подменять КП и не мешать лабораторию с назначением ЛС (Rceth).

---

## 6. Риски

- Джойн только в календарный день визита режет покрытие вдвое.
- Писать `value` в `exam_data` сломает №55.
- Тяжёлый EXISTS по 2 млн строк без `(patient_id, date)` - только склад.
- PHI: значения только на `/var/data`, не в git/логах/handoff.

## 7. Следующая команда

Волна 2: shadow-сверка «Рекомендации по обследованию» ↔ `type_name`.
Night hook и primary - позже. Не включать `MO_LAB_IN_PRIMARY`.
