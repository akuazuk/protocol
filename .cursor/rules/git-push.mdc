---
description: После коммита изменений в этом репозитории выполнять git push origin текущей ветки.
alwaysApply: true
---

После любого осмысленного коммита в проекте Protocol завершай работу командами `git push` (ветка по умолчанию `main`, если не указано иное).
