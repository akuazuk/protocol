[project]
name = "protocol-rag"
version = "0.1.0"
description = "RAG-навигатор по клиническим протоколам Минздрава РБ"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

# Ruff: основная конфигурация в ruff.toml (исторически отдельный файл для CI).
