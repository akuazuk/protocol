# Извлечение текста из PDF (полный текст; без pymupdf индекс неполный)
# Версия синхронизирована с requirements-corpus-pipeline.txt
pymupdf>=1.24.0

# Пайплайн после обновления PDF:
#   python3 extract_corpus.py
#   python3 build_chunks.py
#   python3 build_structured_index.py
#   python3 build_semantic_embeddings.py   # опционально, нужен torch

# Семантические эмбеддинги (опционально, для embeddings.json)
sentence-transformers>=6.0.1,<6.1
torch>=2.14.0,<2.15
transformers>=4.36.0,<4.46
