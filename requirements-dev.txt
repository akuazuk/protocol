# Локальный pytest и HTTP-клиент для TestClient (не обязателен для прод-деплоя)
pytest>=8.0.0
httpx>=0.27.0
# Линтер (используется в CI: .github/workflows/ci.yml)
ruff>=0.15.0
# Git-хуки (опционально): pre-commit install
pre-commit>=3.7.0
