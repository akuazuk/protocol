## scripts/data/py

Canonical Python entrypoints for data/corpus utility scripts.

These wrappers execute legacy top-level `*.py` files via `runpy` to preserve
full backward compatibility during staged migration.
