# План: чистка навигатора протоколов + LLM-переизвлечение (v1)

- Дата: 2026-07-20
- Статус: active
- Ветка работ: `docs/session-plan-2026-07-20` (планы), продовые изменения - в `main`
- Автор итерации: сессия агента 2026-07-20

## 1. Контекст и проблема

В навигаторе протоколов Минздрава (proto-viewer, поиск «На приёме», карточка протокола)
было **много мусора**: юридическая обвязка («Национальный правовой Интернет-портал...»,
«Признать утратившим силу...», подписи министра, названия облисполкомов), заголовки глав,
обрывки предложений. Пункты попадали не в свои разделы.

Первопричины:
1. **Регекс-извлечение** (`auto_extracted`): 373 из 467 протоколов размечены регулярками по
   сырым чанкам PDF - отсюда обрывки ±40 символов, склейки списков, неверные разделы.
2. **Добивка разделов** сырыми чанками, когда LLM-сводка была разреженной.
3. **Неполный фильтр** boilerplate (не хватало белорусских юр-маркеров).

## 2. Что изменено в проде (main) сегодня

| Коммит | Суть |
|--------|------|
| `225d504` | Layer 1: фильтр юр/адм boilerplate во всех путях навигатора |
| `0e295c5` | Очередь `reextract_auto_queue.json` (373 id) + безопасный ребилд RAG jsonl на publish |
| `fa12a5d` | `REEXTRACT_STATE` override - resume-состояние на персистентном диске Render |

Затронутые модули: `clinical_knowledge/extract_quality.py` (общий `is_legal_admin_text`),
`chunk_tags.py`, `protocol_brief.py`, `protocol_kz_brief.py`, `rich_chunk_search.py`,
`scripts/reextract_weak_summaries.py`.

Слои решения:
- **Слой 1 (детерминированный фильтр):** расширены списки boilerplate (в т.ч. белорусские
  юр-маркеры) + regex подписи министра/«ГЛАВА N»; единый `is_legal_admin_text` применён в
  brief / card / nav.
- **Слой 2 (логика добивки):** разделы добиваются только до `min_points_per_section=3` и
  только отфильтрованными клиническими предложениями, а не сырыми чанками до `max_points=6`.
- **Слой 3 (LLM-переизвлечение):** идёт сейчас (см. ниже).

## 3. Что делается сейчас

LLM-переизвлечение 373 `auto_extracted` протоколов на Render Web Shell (Gemini; локально из
Беларуси Gemini geo-blocked, Anthropic без кредитов - поэтому запуск на Render).

- 4 параллельных воркера, `--publish --resume`, state и логи на `/var/data`.
- Резервное копирование извлечённых JSON в `/var/data` каждые 5 минут (rsync).
- **Прогресс на 19:30:** 84 / 373 (~23%); `llm_extractor` 80, `structured_fallback` 4;
  валидация: 55 valid, 29 needs_human_review; память 3.49 / 4 ГиБ; ETA ~3 ч.
- Пока идёт батч - **на main не деплоим** (редеплой Render убьёт воркеры).

## 4. Метрики: было / стало / цель

| Показатель | Было | Цель | Стало (факт, r23) |
|------------|------|------|-------------------|
| Покрытие LLM-качеством | 92/467 (20%) | ≥ 443/467 (~95%) | **451/467 (~96.6%)** |
| Юр/адм мусор в `summary_chunks.jsonl` | частый | ~0 | **0 маркеров** |
| Юр/адм мусор в разделах навигатора | частый | ~0 | **0 (смоук I80)** |
| Доля `structured_fallback` | - | ≤ 5% | **14/465 (~3%)** |
| Строк в `summary_chunks.jsonl` | 4171 | - | **3749 (богаче, +2863/-3285)** |
| Тесты навигатора/качества | - | зелёные | **33/33 pass** |

Примечание: число выдержек до/после почти не меняется (10 -> 10). Выигрыш - в **точности,
структуре и чистоте**, а не в количестве.

## 5. Шаги

### Сделано
- [x] Слой 1: общий `is_legal_admin_text` + расширенный boilerplate, применён в brief/card/nav.
- [x] Слой 2: добивка только клиническими отфильтрованными предложениями, ниже cap.
- [x] Тесты: юр-boilerplate исключён из brief/expanded/card/nav.
- [x] Проверка на ТГВ (I80) + 2 протоколах: мусор ушёл.
- [x] Очередь 373 auto-протоколов (`reextract_auto_queue.json`).
- [x] Безопасный ребилд `summary_chunks.jsonl` из всех опубликованных сводок на publish.
- [x] `REEXTRACT_STATE` override + backup на `/var/data`.
- [x] Коммит+пуш Слоёв 1-2 и инфраструктуры (main): `225d504`, `0e295c5`, `fa12a5d`.

### Сделано (продолжение)
- [x] Батч LLM-переизвлечения 373 протоколов (Render) - завершён 373/373.
- [x] Публикация 460 свежих сводок в `yaml/` (воркеры шли без `--publish` - допубликовал отдельным шагом).
- [x] Пересобран `summary_chunks.jsonl` из всех опубликованных сводок (4171 -> 3749).
- [x] Данные скачаны с Render (rsync) в репозиторий.
- [x] `BUILD_VERSION` -> `2026-07-20-r23-reextract-auto-quality`, коммит `e038583`, пуш в `main` (деплой).
- [x] Смоук: extractor-распределение 451 llm / 14 fallback; 0 юр-маркеров; тесты 33/33; brief I80 чистый.

### Дальше (следующая итерация, v2)
- [ ] Повторно прогнать 14 `structured_fallback` (форс-режим, без resume-skip).
- [ ] Ручное ревью для `needs_human_review` (флаг, не брак).
- [ ] Убедиться, что воркеры Web Shell завершены и /var/data почищен от временных state/log.

## 6. Риски и меры

- **Редеплой во время батча убьёт воркеры** -> state и backup на `/var/data`, resume; на main
  не пушим до конца.
- **Эфемерная ФС Render** -> `REEXTRACT_STATE` и rsync-backup каждые 5 минут.
- **Потеря чанков не-переизвлечённых протоколов при ребилде RAG** -> `write_summary_rag_jsonl()`
  вызывается без списка, перечитывает все опубликованные сводки с диска.
- **Гео-блок Gemini / кредиты Anthropic** -> запуск из разрешённой локации (Render).
- **`needs_human_review` ~35%** -> это флаг на ревью, данные всё равно лучше регекса; ревью - отдельной задачей.
