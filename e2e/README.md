# End-to-end тесты (браузер)

Здесь планируются сценарии уровня UI (например Playwright): открытие приложения, ввод запроса, проверка ответа.

Пока каталог зарезервирован под будущую настройку (`package.json`, `playwright.config.ts`). Юнит- и API-смоук-тесты лежат в `tests/`.
