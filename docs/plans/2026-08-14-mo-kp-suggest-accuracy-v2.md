# МО: точность подбора КП - возраст и срок действия

Статус: **active**  
Дата: 2026-08-14  
Преемник: `2026-08-14-mo-kp-suggest-accuracy-v1.md`  
База: merge `#149` (`8d69404`), прод `2026-08-14-090123Z-kp-adult-pop-fix`.

## Контекст

После выкладки каскада «диагноз → МКБ → жалобы» прогон июля (выборка 300)
дал 57% clinical hit и 43% честного «нет протокола». Два правила, без которых
точность не сдвинется:

1. **Возраст на дату визита.** Сейчас suggest часто не знает возраст: в витрине
   `fact_mo_case` его нет, поэтому 16/172 top-1 были `дет_нас`. Возраст уже есть
   в выгрузке МИС (`patient_bdate`, иногда `patient_age_years`).
2. **КП, действовавший в день визита.** Сейчас matcher выкидывает все
   `status=superseded` оптом и лишь слегка штрафует старый год постановления.
   Нужно не искать отменённый КП, если он отменён **до** даты визита. Если визит
   был ещё при действующем старом посте - этот КП допустим.

Омнибус 2011 (урология №920) в топе июля - типичный случай: старый документ
не должен конкурировать с действующим, если его уже заменили.

## Правило возраста

Источник, по приоритету:

1. Готовый `patient_age_years` / `age_years`, если число 0-120.
2. Иначе **считать** от `patient_bdate` (колонка МИС / `mis_data` / CSV
   `secure_cases`) и **даты визита** (`visit_date` / `date`), не от «сегодня»:
   полных лет на день приёма.
3. Нет ни возраста, ни ДР → аудитория `unknown`.

Порог: &lt;18 лет - ребёнок, иначе взрослый.

Поиск:

- взрослому не предлагать КП `дет_нас` / population=child;
- ребёнку не предлагать только-взрослый КП, если есть детский clinical hit;
- при `unknown` не ставить детский КП top-1, если есть взрослый или `any`
  с тем же clinical-сигналом.

Не печатать ДР и возраст в логах, PR и handoff.

## Правило срока действия КП

Для каждого кандидата и даты визита V:

```text
в силе на V  =
  (valid_from пусто или valid_from <= V)
  и (valid_to пусто или valid_to >= V)
  и не (отменён/заменён и дата_отмены < V)
```

- Отменён **до** визита → не искать, не показывать, не оценивать план по нему.
- Отмена **после** визита или визит внутри срока → старый КП можно брать
  (историческая оценка).
- Нет дат отмены, только «файл пропал с сайта МЗ» (`kp_sync` superseded) -
  не считать юридической отменой; только пометка `sync_missing`, не hard-drop.
- Юридическая отмена: `approval.valid_to`, текст «признать утратившим силу»,
  явная замена новым постом (номер/дата преемника в `replaces` / карточке).

Сейчас в карточках `valid_to` часто пустой, `recency_multiplier` только снижает
балл старого года. Этого мало: урология 2011 остаётся в поиске.

Нужен слой дат на карточке (из поста МЗ / daily sync), затем hard-filter в
`match_protocol_cards` по `visit_date`.

## Что уже в проде (из v1)

- Нет specialty-filler без clinical hit; I84→K64; content-index; `дет_нас` в
  имени файла задаёт population.
- I84.9 / геморрой → КП №22.

## Метрики

| Метрика | Прогон июля (v1) | CSV 26.07-13.08 | Цель |
|---------|------------------|-----------------|------|
| Clinical hit | 57.3% (172/300, путь МКБ) | 71.2% (5411/7605) | ≥75% на CSV с текстом Dx |
| Пусто | 43% честно | 28.8% (2194) | пусто только если нет КП в силе на дату визита |
| `дет_нас` top-1 у взрослых | 16/172 (возраст не знали) | 6 | 0 |
| Омнибус 2011 / диспансеризация | 18 + 17 | 568 top-1 (10.5% available); ЛОР 2017 ×426, урология 2011 ×81 | 0, если отменены до визита; иначе ≤5% без нозологии |
| Возраст решён (число или ДР) | не меряли | 98.8% (7511/7605) | ≥95% clinical CSV |
| КП вне срока в top-1 | не меряли | не считали отдельно (repealed без даты не hard-drop) | 0 |
| Путь запроса | почти все ICD | diagnosis 7498 / icd 64 / complaints 43 | диагноз-first |

## Шаги

1. [x] Возраст в suggest: `age` если валиден, иначе `bdate` + `visit_date`.
   Прокинуть в `build_case_fact_graph` / `adult_or_child`. Тест: 7 лет → детский
   орто КП; 41 год → не `дет_нас`.
2. [x] Даты силы КП на карточке: `valid_from`, `valid_to`, `superseded_on`,
   `replaced_by`. Заполнить из поста/sync, где есть; пустое не выдумывать.
3. [x] Hard-filter: не искать КП, отменённый до даты визита. Визит до отмены -
   старый КП допустим. Тест: визит 2026-07 vs КП 2011 с `valid_to` &lt; визита → нет
   в выдаче; визит 2012 → можно.
4. [x] Демоут омнибус без нозологии в содержании, даже если ещё формально в силе
   (урология 2011, «все ЛОР/кожа», диспансеризация на ненулевой главе МКБ).
5. [x] Пустые кластеры июля, но только среди КП в силе: J06, K21, K30, M54, E03;
   A63/B07 - честное «нет» или дерм-КП, не омнибус 2011.
   K21/K30 → КП пищевод/желудок 2025; E03 → щитовидная железа 2021.
   J06, M54, A63, B07 - честное «нет» (нет действующего нозологического КП).
6. [x] CSV-прогон 26-31.07 и август: Dx + жалобы + возраст + filter по дате визита.
   GCE `/var/data/medical_exams/reports/kp_suggest_eval_2026-07-26_08-13.json`
   (2026-08-14T20:26:47Z, 32395 с, 7605 clinical). Hit 71.2%, возраст 98.8%.
   Хвост: омнибус ЛОР 2017 и 6× `дет_нас` у взрослых.
7. [x] Скорость матчера (префильтр), иначе полный месяц непрактичен.
8. [x] Golden 40 (верно/неверно) с возрастом и датой визита; без PHI в git.
   Fixture `tests/fixtures/mo_kp_suggest_golden.jsonl`: возраст или ДР+визит,
   Hit@k где нозология не top-1, честный reject омнибуса, `specialty_only_no_filler`.
9. [x] Не возвращать specialty-filler (кейс `specialty_only_no_filler`).

## Риски

- Пустой `valid_to` у большинства карточек: пока нет даты отмены, hard-drop
  нельзя - только recency + омнибус-демоут.
- Путать «нет на сайте» и «утратил силу» - разные вещи.
- ДР считать только на дату визита; не писать ДР в отчёты.
- PR #146 (Rceth) не трогать.

10. [x] Омнибус ЛОР/урология: не считать clinical по dump `icd10_all`.
    Только title/path. J06/ОРВИ не должны брать КП «оториноларингологическими
    заболеваниями» 2017.

## Следующая команда

После merge: прогон CSV на GCE (цель hit ≥75%, omnibus top-1 ≤5%).
Не смешивать с Rceth deploy.
